numpy>=1.24
pandas>=2.0
matplotlib>=3.7
MetaTrader5>=5.0.45   # only needed to pull live Deriv data (fetch_diverse.py)
# tvdatafeed is not on PyPI; install separately for fetch_tradingview.py:
#   pip install --no-cache-dir "git+https://github.com/rongardF/tvdatafeed.git"
