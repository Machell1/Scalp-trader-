# v1.30 FTMO pass-policy compiled-kernel repair — pre-registration

The unchanged object-level Python reference measured 186.310161 seconds for one
path across C0/C1/P1, including 564,300 account-rail checks. At that rate the
registered 100,000-path experiment is infeasible. Numba 0.66.0 was installed in
the user Python 3.13 environment as a research-runner dependency only.

Implement a primitive-array Numba kernel for the exact existing state machine:
calendar and EA-day resets; pending occupancy and suppression; current-balance
lot sizing and broker floors; entry slippage; marks; two-stop conservative
equity; partial rounding; swaps; final-deal loss streaks; target freeze; FTMO
daily/static breach; EA daily/peak/static halts; fill/consecutive/global/cluster
caps; sequential phase reset; minimum trading days; and calendar timeout.
Bootstrap source indices remain generated by the existing
`CompiledBootstrap.source_indices`, preserving seed and path-ID streams exactly.

Frozen: 100,000 IDs 0..99,999; seed 13020260711; 20-day moving blocks; C0/C1/P1;
CRN; E1/E2 tapes and costs; two-stop equity; FTMO/EA parameters; `RESULT_DTYPE`;
all Wilson, timeout, completion-time, paired and divergence gates; ledger 211;
development-only exposure. No strategy, data, risk, bootstrap, output, or gate
change is allowed.

Equivalence gate before full execution: for both E1 and E2 and path IDs 0 and
137, compare the compiled and Python-reference rows for all three policies.
Every `RESULT_DTYPE` field must match exactly; floating fields must also have
identical IEEE-754 bytes. Any difference stops the full run and is reported.
The existing synthetic suites and golden regression remain mandatory. The full
command remains `python -u backtest/run_v130_pass_policy.py --development`.

Hypothesis charge: zero. This is execution-only and produces no additional
policy cell. No blind-frame unlock or trading-terminal access is permitted.

**PRE-REGISTRATION ENDS — hash all UTF-8/LF bytes through this line, including its newline.**

**Recorded protocol SHA256:** `79126053eeaae33174e93aaf292cd636a4b787ecfe0c1d5dd94761330f5c5428`
